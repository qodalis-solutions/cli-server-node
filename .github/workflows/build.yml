name: Build & Test

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [develop]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          registry-url: "https://registry.npmjs.org"

      - name: Install dependencies
        run: npm ci

      - name: Build abstractions
        run: cd packages/abstractions && npx tsc

      - name: Install plugin dependencies
        run: |
          cd plugins/data-explorer-sql && npm install --omit=dev
          cd ../data-explorer-mongo && npm install --omit=dev
          cd ../data-explorer-postgres && npm install --omit=dev
          cd ../data-explorer-mysql && npm install --omit=dev
          cd ../data-explorer-mssql && npm install --omit=dev
          cd ../data-explorer-redis && npm install --omit=dev
          cd ../data-explorer-elasticsearch && npm install --omit=dev
          cd ../admin && npm install
          cd ../aws && npm install

      - name: Build plugins
        run: |
          cd plugins/filesystem && npx tsc
          cd ../weather && npx tsc
          cd ../filesystem-json && npx tsc
          cd ../filesystem-sqlite && npx tsc
          cd ../filesystem-s3 && npx tsc
          cd ../jobs && npx tsc
          cd ../data-explorer && npx tsc
          cd ../data-explorer-sql && npx tsc
          cd ../data-explorer-mongo && npx tsc
          cd ../data-explorer-postgres && npx tsc
          cd ../data-explorer-mysql && npx tsc
          cd ../data-explorer-mssql && npx tsc
          cd ../data-explorer-redis && npx tsc
          cd ../data-explorer-elasticsearch && npx tsc
          cd ../admin && npx tsc
          cd ../aws && npx tsc

      - name: Build
        run: npm run build

      - name: Test
        run: npm test
